/**
 * Public site toggles. Flip these when repos go public or live URLs are ready.
 * Admin / CMS fields stay available either way.
 */
/** Keep false until repos are public — Admin can store URLs meanwhile. */
export const SHOW_PROJECT_GITHUB = false;
/** AgriConnect (and any project with liveUrl) can show Live when true. */
export const SHOW_PROJECT_LIVE = true;
/** Hide org GitHub from footer / contact until you want it public. */
export const SHOW_PUBLIC_GITHUB = false;
